rootProject.name = "sample-wasi-http-kotlin"

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven("https://packages.jetbrains.team/maven/p/kt/dev")
        // mavenLocal()
    }
}
